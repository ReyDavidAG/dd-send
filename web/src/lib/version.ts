// Versión desplegada. Se muestra en la UI al lado del logo para confirmar
// en qué build está el usuario. Mantener en sincronía con `package.json`
// cada vez que se bumpea.
export const VERSION = "1.2.0";